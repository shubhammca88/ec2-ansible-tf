---
- name: Install nginx
  hosts: test
  become: yes
  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install nginx
      apt:
        name: nginx
        state: latest

    - name: Start nginx service
      systemd:
        name: nginx
        state: started
        enabled: yes       # when system restart